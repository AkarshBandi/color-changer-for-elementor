# Composer dependencies (dev-only tooling)
/vendor/

# PHPUnit cache
.phpunit.result.cache

# PHPCS cache
.phpcs-cache

# Pre-release backup files (e.g. class-x.php.bak-2.1.6) kept locally only
*.bak-*
